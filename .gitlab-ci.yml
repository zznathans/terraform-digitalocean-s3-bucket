stages:
  - test
  - build
  - deploy
  - release

before_script:
  - export TERRAFORM_MODULE_SYSTEM=digitalocean

include:
include:
  - component: $CI_SERVER_FQDN/ci/semantic-release/release@~latest
  - template: Terraform-Module.gitlab-ci.yml
